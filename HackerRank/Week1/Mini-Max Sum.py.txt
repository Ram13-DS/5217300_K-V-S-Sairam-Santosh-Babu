
import math
import os
import random
import re
import sys


def miniMaxSum(arr):
    re=[]
    s=sum(arr)
    for i in range(len(arr)):
        re.append(s-arr[i])
    print(min(re), max(re))

if __name__ == '__main__':

    arr = list(map(int, input().rstrip().split()))

    miniMaxSum(arr)
